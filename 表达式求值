#include <iostream>
using namespace std;
int main()
{
    int a,b,c;
    cin>>a>>b>>c;
    int x1=a+b+c;
    if(x1<a+(b*c))
        x1=a+(b*c);
    if(x1<a*(b+c))
        x1=a*(b+c);
    if(x1<a*b*c)
        x1=a*b*c;
    if(x1<(a+b)*c)
        x1=(a+b)*c;
    cout<<x1;
    return 0;
}
